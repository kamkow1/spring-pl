def fibonacci_recursive_helper(a, b, counter, len) do
    if $counter <= $len do
        println $a;

        fibonacci_recursive_helper $b, ($a + $b), $counter + 1, $len; 
    end

end

def fibonacci_recursive(len) do
    -- start the sequence
    fibonacci_recursive_helper 0, 1, 1, $len;
end