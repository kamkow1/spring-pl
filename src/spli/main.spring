def fibonacci_helper(f, b, c, len) do
    if $c <= $len do
        println $f;
        fibonacci_helper $b, ($f + $b), ($c + 1), $len; 
    end

end

def fibonacci(len) do
    fibonacci_helper 0, 1, 1, $len;
end

fibonacci 10;