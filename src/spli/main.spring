[]
    include "./to_import.spring";
[/]

def dostuff(arr) do
    println $arr[1];
end

def main(args) do
    -- fibonacci int $args[0];

    each "arg" inside $args do
        if $arg == "exit" do 
            bail out;
        end

        println $arg;
    end
end