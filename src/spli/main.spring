[][/]

def main() {
	create_server();

	lambda: _, body => {
		$body["roman"] -> $roman;
		println $roman;
		return $roman;
	} -> $demo_post_handler;

	create_endpoint 
		"/demo-post", 
		"post", 
		"demo_post_handler", 
		"ok";

	run_server();
}